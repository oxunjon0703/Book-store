<nav class="navbar bg-dark navbar-expand-lg bg-body-tertiary mb-4" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">BOOK STORE</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link <%= page === 'main' ? 'active' : '' %>" aria-current="page" href="/">Asosiy</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= page === 'authors' ? 'active' : '' %>" href="/authors">Mualliflar</a>
                </li>
                <% if (adminId) { %>
                    <li class="nav-item">
                        <a class="nav-link <%= page === 'orders' ? 'active' : '' %>" href="/orders">Buyurtmalar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= page === 'categories' ? 'active' : '' %>" href="/categories">Toifalar</a>
                    </li>
                    <% } %>
            </ul>
            <% if (!adminId) { %>
                <div class="d-flex">
                    <a class="btn btn-warning" type="submit" href="/login">
                        Login
                    </a>
                </div>
                <% } else { %>
                    <div class="d-flex">
                        <a class="btn btn-success me-2" type="submit" href="/profile">
                            Profile
                        </a>
                        <a class="btn btn-danger" type="submit" href="/logout">
                            Logout
                        </a>
                    </div>
                    <% } %>
        </div>
    </div>
</nav>